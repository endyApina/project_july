import styled from 'styled-components';
import { TouchableOpacity } from 'react-native';

export const SummaryScreenContainer = styled.View`
  flexDirection: row;
  marginLeft: 1%;
`;

export const Touch = styled(TouchableOpacity) `
  width: 50%;
  marginRight: 2%;
`;