import styled from 'styled-components/native';
import {Dimensions} from 'react-native';

export const RegsitrationContainer = styled.View`

    background-color: ${({ bgcolor })=> bgcolor};
    height: 100%;
    padding: 20px;
`;